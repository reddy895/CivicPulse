{
  "name": "civicpulse-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "CivicPulse DPG - Multilingual AI Infrastructure Alignment Platform",
  "license": "AGPL-3.0-or-later OR Apache-2.0",
  "author": "Praveen Reddy",
  "scripts": {
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"cyan,magenta\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "start": "concurrently --kill-others -n \"backend,frontend\" -c \"cyan,magenta\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
