import React from 'react';
import { useAuth } from '../context/AuthContext';

// Sidebar is no longer used — navigation is in Navbar.
// This stub prevents import errors from GovernmentDashboard.
export default function Sidebar({ activeTab, setActiveTab }) {
  return null;
}
